<%= button_tag "confirm", class: "btn btn-primary confirm", type: "submit", value: "#{user.id}" %>
<%= button_tag "delete", class: "btn btn-secondary deletereq", type: "submit", value: "#{user.id}" %>